% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/G2G_visualizations.R
\name{plot_insample_holdout_paper}
\alias{plot_insample_holdout_paper}
\title{Paper-Style Actual-vs-Model Survival and Retention Plot}
\usage{
plot_insample_holdout_paper(
  fo,
  data,
  subject,
  split_at,
  group = NULL,
  model_type = c("auto", "varying", "static")
)
}
\arguments{
\item{fo}{Formula of the form \code{Surv(time, status) ~ covariates}.}

\item{data}{Full panel data frame containing both training and test rows.}

\item{subject}{Character string naming the subject ID column.}

\item{split_at}{Numeric scalar. Observations with time at or below this
value are used for training; the remainder form the hold-out set.}

\item{group}{Optional character string naming a grouping column for
faceted plots. Use \code{NULL} (default) for an overall-only plot.}

\item{model_type}{One of \code{"auto"} (default), \code{"varying"}, or
\code{"static"}.}
}
\value{
A named list with elements \code{p_survival} (a \code{ggplot}
  of percent surviving), \code{p_retention} (a \code{ggplot} of
  retention rate), and \code{solution} (the fitted model object).
}
\description{
Fits the G2G model on the training portion of the data (rows where
\code{time <= split_at}) and produces two plots comparing the
Kaplan-Meier actual curve against G2G model predictions: one for
percent surviving and one for period-over-period retention rate.
An optional grouping variable produces faceted panels.
}
